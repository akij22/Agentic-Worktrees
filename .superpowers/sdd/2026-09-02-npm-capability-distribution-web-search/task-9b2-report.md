# Task 9B2 — Transactional managed Capability removal

Status: **COMPLETE.** This gate implements managed npm Capability removal only. Bundled URL Fetch remains non-removable and no later task was started.

## Contract and lifecycle

Removal is a two-call, single-consent-lease operation. Inspection returns the immutable, path-free tuple `(inspectionId, packageName, capabilityId, activeVersion, activeIntegrity, activeContentDigest, activeRunCount, expiresAt)`. Acceptance must repeat the exact inspection ID, package name, active version, and active-run count. The global `PackageLock` remains owned across inspection, consent, validation, deactivation, commit, rollback/recovery, cleanup, and terminal release.

Before mutation, removal verifies a managed installed Capability, catalog identity, exact installation/configuration/session snapshots, and idle active runs. It deactivates all reviewed active runs, detaches the pointer reversibly, atomically removes the installation/configuration, refreshes the catalog, and stages only unreferenced version directories for garbage collection. Successful removal never auto-activates a session.

The durable removal-recovery row contains relative/serializable snapshots and GC digests. Pointer, database, catalog, GC, or runtime failure restores the exact pointer, installation, configuration, settings, sessions, and staged versions. Conflicting restoration is quarantined rather than overwriting external state. Secret references and chat associations remain through rollback; secrets are deleted only during successful cleanup. Cleanup failure remains `cleanup_pending` and startup reconciliation retries it without activation.

## Files

- `src/shared/packages/schemas.ts` — strict removal inspection/acceptance DTOs and optional active-run progress count.
- `src/shared/packages/removal-recovery.ts` — strict path-free removal recovery schema.
- `src/shared/packages/update-recovery.ts` — exports shared serializable configuration/session snapshot primitives.
- `src/shared/db/schema.ts` — removal recovery table definition.
- `src/main/database/bootstrap.ts` — bootstrap/legacy creation of the recovery table.
- `src/main/database/index.test.ts` — database upgrade table assertion.
- `src/main/database/migrations/0012_big_shooting_star.sql`, `meta/0012_snapshot.json`, `meta/_journal.json` — generated by `npm run db:generate`; not manually edited.
- `src/main/packages/package-repository.ts` — owner-checked removal journal creation, transitions, completion, quarantine, and operation completion.
- `src/main/capabilities/capability-removal-installer.ts` — reversible pointer/DB/catalog/GC commit, exact restoration, retention analysis, and cleanup.
- `src/main/capabilities/capability-removal-service.ts` — inspection lease, exact consent, idle/deactivation orchestration, rollback/quarantine, secret cleanup, and reconciliation.
- `src/main/capabilities/capability-distribution-service.ts` — removal facade and startup reconciliation integration; updates refuse packages with pending removal recovery.
- `src/main/capabilities/capability-removal.test.ts` — 20 direct removal lifecycle cases.
- `src/shared/ipc/schemas.test.ts` — strict removal request boundary and install-intent projection coverage.
- This report and `progress.md` — completion evidence and ledger entry.

`src/renderer/features/coding-agent/components/SessionMessages.tsx` was reverted to HEAD. Its unrelated `skillInvocations` Props addition only addressed the pre-existing renderer typecheck blocker and is not necessary for Task 9B2 removal.

## Direct removal tests (20)

1. Immutable path-free inspection tuple, active count, and no npm/filesystem side effects.
2. Successful pointer/configuration removal, stable-version retention, secret cleanup, and no activation.
3. Bundled URL Fetch refusal without installer or activation calls.
4. Exact active-run-count mismatch refusal.
5. Exact active-version mismatch refusal.
6. Idle-run rejection before persistent mutation.
7. A recording coordinator proves multi-run deactivation receives only both exact reviewed run IDs, persists both as inactive, and never auto-reactivates.
8. Pointer-detach failure exact restoration.
9. Catalog-publication failure restores pointer/DB/catalog; the service supplies the reviewed run IDs to reactivation and the recording coordinator proves that actual argument exactly equals the earlier deactivated set.
10. Later GC failure restores already staged version directories.
11. Reactivation failure retains conflict recovery and blocks installation.
12. A distinct old version referenced by a real inactive session association survives GC recursively (including its executable sentinel), with the exact association unchanged.
13. Distinct previous/candidate versions referenced by a durable update recovery journal both survive GC recursively, with the complete parsed journal (token, stage/error, pointers, integrity/digests, installation, configuration/settings, and sessions) deeply unchanged.
14. Session/chat associations remain exact through inspection cancellation.
15. Cleanup failure persists `cleanup_pending` recovery.
16. Startup reconciliation completes pending cleanup without activation.
17. Removal waits behind a held update PackageLock lease without acquiring or mutating, then completes without deadlock and preserves exact state on cancellation.
18. Frozen schema-valid path-free progress events.
19. Immutable stack-free/path-free terminal errors.
20. Successful cleanup removes journal/staging exactly once.

## Verification

- `npm run db:generate` — 27 tables; **No schema changes, nothing to migrate**, confirming checked-in `0012_big_shooting_star.sql`, `meta/0012_snapshot.json`, and `_journal.json` match the schema.
- `npm test -- src/main/capabilities/capability-removal.test.ts` — **1 file / 20 tests passed**.
- Task 9 regression selection (removal, update lifecycle/recovery/configuration/blocking, installer, discovery/metadata) — **7 files / 110 tests passed**.
- `npm run typecheck` — Task 9B2 source/tests pass type checking; the command remains nonzero only for the documented pre-existing unrelated `CodingAgentSession.tsx:387` missing `skillInvocations` Props diagnostic.
